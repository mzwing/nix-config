# environment

Here stores the example of my personal development environment configuration.

Use [devenv](https://devenv.sh) to manage.

And the `.envrc` file is used with [direnv](https://direnv.net/) to automatically load environment variables when you enter the directory. It is expected to be used with `devenv` to set up the environment variables.
