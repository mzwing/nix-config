name: Update lock files

on:
  push:
    branches:
      - master
    paths:
      - .github/workflows/update.yml
  schedule:
    - cron: "0 13 * * *"
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: update-lockfiles
  cancel-in-progress: false

jobs:
  update:
    name: Update lock files
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Maximize Nix disk space
        uses: wimpysworld/nothing-but-nix@v10
        with:
          hatchet-protocol: cleave

      - name: Set up Nix
        uses: mzwing/nix-actions/nix/setup@master
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          cachix-pull: 'false'
          install-devenv: 'true'

      # One cache family (nix-ci-) shared with build.yml and tests.yml. The key must hash every job's lock inputs and use the same gc size.
      - name: Restore Nix store
        uses: mzwing/nix-actions/nix/restore-store@master
        with:
          primary-key: nix-ci-${{ hashFiles('flake.lock', 'devenv.lock', 'devenv.nix', 'devenv.yaml') }}
          restore-prefix: nix-ci-
          gc-max-store-size-linux: 4G

      - name: Update devenv inputs
        run: devenv update

      - name: Update flake inputs
        shell: devenv shell bash -- -e {0}
        run: just up

      # After the flake inputs, so the resolver matches the agent-skills revision this commit pins.
      - name: Update skill sources
        shell: devenv shell bash -- -e {0}
        run: just skills-update

      - name: Commit and push changes
        id: commit
        uses: mzwing/nix-actions/git/commit-and-push@master
        with:
          paths: devenv.lock flake.lock data/skills/sources.lock.json
          message: "chore: update lockfile"
          trigger-workflow: build.yml
          github-token: ${{ github.token }}
