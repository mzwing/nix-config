name: Set up Nix
description: >-
  Install Nix with the shared substituter configuration, optionally
  configure Cachix for pulls and install devenv.

inputs:
  github-token:
    description: >-
      GitHub token for `access-tokens` (private flake inputs). Leave empty
      to skip.
    required: false
    default: ''
  extra-substituters:
    description: >-
      Additional substituters (space-separated), appended to the base list.
    required: false
    default: ''
  extra-trusted-public-keys:
    description: >-
      Additional trusted public keys (space-separated), appended to the base
      list.
    required: false
    default: ''
  max-jobs:
    description: >-
      Value for the max-jobs nix setting (e.g. "0" on coordinators). Leave
      empty to skip.
    required: false
    default: ''
  cachix-name:
    description: Cachix cache name to configure for pulls.
    required: false
    default: mzwing
  cachix-pull:
    description: Whether to configure Cachix for pulls (skipPush).
    required: false
    default: 'true'
  install-devenv:
    description: Whether to install devenv into the nix profile.
    required: false
    default: 'false'

runs:
  using: composite
  steps:
    - name: Assemble extra_nix_config
      id: nix-config
      shell: bash
      env:
        GITHUB_TOKEN_INPUT: ${{ inputs.github-token }}
        EXTRA_SUBSTITUTERS: ${{ inputs.extra-substituters }}
        EXTRA_TRUSTED_PUBLIC_KEYS: ${{ inputs.extra-trusted-public-keys }}
        MAX_JOBS: ${{ inputs.max-jobs }}
      run: |
        set -euo pipefail
        {
          echo 'extra_nix_config<<NIX_CONFIG_EOF'
          echo 'accept-flake-config = false'
          if [[ -n "${GITHUB_TOKEN_INPUT}" ]]; then
            echo "access-tokens = github.com=${GITHUB_TOKEN_INPUT}"
          fi
          if [[ -n "${MAX_JOBS}" ]]; then
            echo "max-jobs = ${MAX_JOBS}"
          fi
          echo "substituters = https://mzwing.cachix.org https://cache.nixos.org https://nix-community.cachix.org https://attic.xuyh0120.win/lantian ${EXTRA_SUBSTITUTERS}"
          echo "trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= mzwing.cachix.org-1:tOO3NqAwrXyPCnecEl/0wXwparCRksM5TeuS/wZK+KA= nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs= lantian:EeAUQ+W+6r7EtwnmYjeVwx5kOGEBpjlBfPlzGlTNvHc= ${EXTRA_TRUSTED_PUBLIC_KEYS}"
          echo 'NIX_CONFIG_EOF'
        } >>"${GITHUB_OUTPUT}"

    - name: Install Nix
      uses: cachix/install-nix-action@v31
      with:
        extra_nix_config: ${{ steps.nix-config.outputs.extra_nix_config }}

    - name: Configure Cachix for pulls
      if: inputs.cachix-pull == 'true'
      uses: cachix/cachix-action@v17
      with:
        name: ${{ inputs.cachix-name }}
        skipPush: true

    - name: Install devenv
      if: inputs.install-devenv == 'true'
      shell: bash
      run: nix profile add nixpkgs#devenv
